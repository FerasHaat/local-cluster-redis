redis:
  architecture: standalone
  auth:
    enabled: false
  global:
    security:
      allowInsecureImages: true
  image:
    repository: bitnamilegacy/redis
  master:
    persistence:
      enabled: false
